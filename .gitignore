# Dataset (large binary files -- not tracked; see README for download/setup instructions)
dataset/
archive*.zip
*.zip

# Trained model and generated outputs (plots, reports, confusion matrix,
# predictions) ARE tracked -- they're the graded deliverables (Step 10) and
# are required for evaluators to run predict.py/app.py without retraining.
# Only large raw input data below stays excluded.

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# Virtual environments
venv/
.venv/
env/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
